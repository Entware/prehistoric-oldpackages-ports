--- a/src/file.c
+++ b/src/file.c
@@ -25,7 +25,7 @@
 	char *h, *i;
 
 	h = getenv("HOME");
-	i = getenv("IMAPFILTER_HOME");
+	i = "/opt/etc/imapfilter";
 
 	if (i == NULL)
 		n = strlen(h ? h : "") + strlen(h ? "/" : "") +
